# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python package

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule: 
    - cron: '30 1 * * *'

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11"]

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        
    - name: Install library
      run: |
        pip install requests
        pip install beautifulsoup4
        pip install lxml
        pip install pandas
        pip install selenium
        pip install webdriver_manager
      
   # 기존 workflow 파일의 내용...

    - name: run file
      run: |
        python3 lezincomics.py
        python3 naverwebtoon.py

    # 크롤링 작업이 모두 완료된 후에 실행되어야 함
    - name: Save data to JSON file
      run: |
        import json

        try:
            # 데이터 처리 로직
            data = {"example": "data"}
            with open('data.json', 'w') as f:
                json.dump(data, f)
            echo "Data saved to data.json"
        except Exception as e:
            echo "Error occurred: $e"

    - name: commits
      run : |
        git config --global user.email "nys060121@naver.com"
        git config --global user.name "skadbstj12" 
        git add lezincomics_*.json
        git add naverwebtoon_*.json
        git commit -m "차트 수집 완료"

    - name: push
      uses: ad-m/github-push-action@master
      with:
        branch: "main"
        github_token: $



        
